Tạo AccessTokenSecret bằng thư viện crypto:

1. Node
2. require('crypto').randomBytes(64).toString('hex')

# Backend: Node + ExpressJS + JavaScript + MongoDB + Mongoose + Bcrypt + Jsonwebtoken + CORS + Dotenv + Cookie-parser

# Frontend: React + TypeScript + TailwindCSS + react-router + Axios + Zustand + Zod + react-hook-form + @hookform-resolvers + sonner + lucide-react + shadcn ui
